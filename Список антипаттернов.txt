
------------------------ Группа 1: "Тяжелая Артиллерия" (Серьезные Антипаттерны) -----------------------

                                 Архитектура

#81 God Object (Класс делает всё) — God_Controller_Main управляет UI, логикой и данными.

#19 Global State (Глобальное состояние) — Global_Data_Dump доступен отовсюду и изменяем.

#100 Big Ball of Mud (Хаос) — Вся система связна, нет слоев, спагетти-зависимости.

#85 Spaghetti Code (Смесь логики) — В main перемешаны println, switch и бизнес-логика.

#62 Circular Dependency (Циклическая зависимость) — MathA зависит от MathB и наоборот.

#77 Inner Platform Effect (Свой язык) — Метод eval("SUM") парсит строки вместо кода.

#28 Magic Container (Map вместо объекта) — Настройки хранятся в HashMap CFG.

                             ООП и Наследование

#63 Yo-Yo Problem (Прыгающая логика) — calc использует методы из Rube, Mid и Abstract классов.

#18 Data Class (Класс-структура) — Global_Data_Dump только хранит поля, логики нет.

#22 Temporary Field (Временное поле как глобальное) — iterator_i глобален, но нужен только для цикла.

#64 Call Super (Обязательный super) — Если в calc не вызвать super, результат неверен.

#36 Utility Class Public Constructor (Ошибка утилит) — Spaghetti_Security можно инстанцировать.

#71 Interface Bloat (Лишние методы) — IMathGod заставляет писать 4 пустых метода.

#50 Refused Bequest (Наследование интерфейса ради констант) — implements I_Enterprise_Const.

                           Данные и Безопасность

#25 Golden Hammer (String для всего) — Данные хранятся как "ID|Имя|Оценка".

#26 Primitive Obsession (Примитивы вместо типов) — Использование Vector<String> вместо List<Student>.

#37 Hardcoded Secret (Пароль в коде) — MASTER_KEY = "12345".

#17 Hardcoded Path (Путь к файлу) — C:\Windows\Temp... (сломается на Linux).

#31 Object Cesspool (Грязный пул) — POOL возвращает StringBuffer с мусором.

#30 Public Static Mutable Collection (Дыра безопасности) — AUDIT_LOG открыт всем.

#21 Obsolete Collection (Старье) — Использование Vector.

                            Обработка ошибок

#27 Swallowing Exception (Скрытие ошибки) — Пустой catch в static блоке.

#93 Pokemon Exception Handling (Ловля всего) — catch (Exception e) в do_CALC.

#88 Exception for Flow Control (Ошибка как goto) — Выход через throw RuntimeException("EXIT").

#40 Destructive Exception Handling (Разрушение данных) — Сброс токена в null при любой ошибке.

#59 Catching NPE (Ленивая проверка) — try { len() } catch (NPE) вместо if != null.

#70 Catching Error (Ловля фатальных сбоев) — Перехват StackOverflowError.

#39 Log and Throw (Дублирование) — Логгируем ошибку и кидаем её дальше.

#51 Return Null (Возврат null) — getUserRole возвращает null, провоцируя NPE.

#54 Null Object Abuse (Неправильный Null Object) — getToken всегда возвращает null.

                          Потоки (Concurrency)

#83 Busy Waiting (Жжем процессор) — Цикл while для ожидания старта.

#75 Race Hazard (Гонка данных) — Global_Data_Dump.l++ не атомарен.

#41 Synchronized on Boolean (Глобальная блокировка) — Блокировка на Boolean.TRUE.

#49 Fire and Forget (Потеря потока) — Логирование в потоке без ссылок.

#50 Thread.stop() Abuse (Убийство потока) — Вызов stop() в emergency_kill.
                        
                          Связность и Логика

#76 Sequential Coupling (Скрытый порядок) — init() обязан быть перед process().

#94 Shotgun Surgery (Дробное изменение) — Расчет требует создания объекта, вызова 3 методов и правки 2 классов.

#42 Feature Envy (Зависть к данным) — DO_LOGIN лезет в RULES глобального класса.

#46 Side Effects in Logic (Побочный эффект) — Метод проверки пароля меняет глобальный токен.

#55 Zombie Code (Зомби) — Метод warmup считает и ничего не делает.

#69 Blowing the Stack (Переполнение стека) — Рекурсия в recursiveAdd.

#23 Flag Argument (Аргумент-флаг) — b_Running управляет всем.

#97 Global Index Loop (Глобальный индекс) — iterator_i используется в for.

#78 Speculative Generality (Обобщение) — complexAlgo(Object...).

#80 Boat Anchor (Якорь) — Метод legacyCalc вызывается "для совместимости".

#92 Blind Faith (Слепая вера) — Нет проверки p на null перед передачей в DO_LOGIN.

#60 Resource Leak (Утечка) — new FileInputStream без закрытия.

#52 Magic Boolean (Магический булеан) — checkIntegrity возвращает true (неясно зачем).

#67 Free Electron (Свободный электрон) — calc не использует состояние объекта Rube.

#68 Downcasting (Приведение) — (AbstractCalc) this.

#72 Telescoping Constructor (Телескоп) — 3 конструктора, вызывающих друг друга.

#73 Side Effects in Constructor (Сайд-эффект) — Конструктор меняет iterator_i.

#31 Stubborn Object (Упрямый объект) — Переиспользование грязного буфера в Spaghetti_Security.

#70 Stubborn Object (Дубль смысла) — то же, что и #31, но в контексте использования.

#82 Throws in Main (Выброс из main) — main не обрабатывает ошибки.


------------------- Группа 2: "Мелочь" и "Плохой стиль" --------------------------

#1 Comments as Perfume (Комментарии скрывают плохой код) — Комментарии в pom.xml.

#2 Mixed Case Package (Нестандартный регистр пакетов) — Com.Legacy.Systems.

#3 Abstract Artifact Name (Имя не говорит о сути) — CORE_MODULE.

#4 Semver Violation (Плохое версионирование) — FINAL_v100_RC2.

#5 No Properties (Версии не в переменных) — Хардкод версий в pom.xml.

#6 Dependency Bloat (Лишние библиотеки) — commons-lang3 ради одного метода.

#7 Legacy Library (Устаревшие либы) — joda-time.

#8 Unused Dependency (Мертвый груз) — log4j.

#9 Duplicated Dependency (Дубли в сборке) — commons-lang3 дважды.

#10 Final Name Hardcode (Имя выходного файла) — <finalName>run_me.

#11 Version Pinning (Жесткая версия плагина) — 3.8.1.

#12 Downgrade (Старая Java) — Компиляция под Java 8.

#14 Screaming Case Naming (Имя файла капсом) — I_Enterprise_Const.

#15 Hungarian Notation (Префиксы типов) — i_ZERO.

#16 Magic String Constant (Странная константа) — Меню в STR_OPTS.

#32 Variable Shadowing (Имя переменной скрывает имя класса/поля) — int Global_Data_Dump.

#33 Confusing Naming (Похожие символы) — l и 1, O и 0.

#34 Double Negative (Двойное отрицание) — isNotDisconnected.

#38 Method Naming Convention (Имя метода капсом) — DO_LOGIN.

#43 Magic Number (Число без объяснения) — Парсинг length < Integer.parseInt(...).

#47 Disemvoweling (Удаление гласных) — Метод encrypt (внутри просто реверс).

#48 Train Wreck (Цепочка вызовов) — reverse(trim(upper(...))).

#57 YAGNI Violation (Код на будущее) — Дженерик метод future.

#58 Premature Optimization (Мнимая оптимизация) — fastString через char массив.

#61 BaseBean (Пустой предок) — class BaseMath.

#65 Field Hiding (Скрытие поля родителя) — factor в MidCalc и Rube.

#66 Variable Shadowing (Локальная переменная скрывает поле) — factor (см. выше).

#79 Unchecked Cast (Каст без проверки) — (Integer) o в getInt.

#86 Switch Statement (Switch вместо полиморфизма) — Меню в God_Controller.

#87 Static Method Abuse (Сплошная статика) — Все методы в God_Controller.

#89 Magic String Usage (Литералы в коде) — "UNKNOWN", "FAIL".

#90 Manual GC (Ручной сборщик мусора) — System.gc().

#91 Single Letter Naming (Переменная из 1 буквы) — String p.

#95 String Concat Logic (Склейка данных) — time + "|" + res.

#96 Long Method / Arrow Code (Вложенность) — do_SAVE.

#98 Unchecked Cast (Каст в цикле) — (String) DB.get.

#99 Commented Out Code (Мертвый код) — // saveToDisk.

#13 Constant Interface (Интерфейс для констант) — I_Enterprise_Const.

#56 Empty Interface Abuse (Интерфейс-маркер) — ISecure.

#84 Infinite Loop (Бесконечный цикл) — while в Main (технически нужен, но сделан криво).

#35 Refused Bequest (Нарушение контракта) — toString возвращает null.

#29 Soft Coding (Логика в строке) — RULES = "MIN_LEN=3...".

#24 Static Initializer Abuse (Логика в static) — Инициализация DB.

#74 Lazy Class (Класс-обертка) — Wrapper.
